{
  "name": "query-generation-module",
  "private": true,
  "version": "1.0.0",
  "description": "Root automation for the Query Generation full-stack app",
  "scripts": {
    "postinstall": "npm --prefix frontend install",
    "client": "npm.cmd --prefix frontend run dev -- --host 127.0.0.1 --port 5173",
    "server": "python backend/run.py",
    "dev": "npm run fullstack",
    "fullstack": "concurrently -k -n backend,frontend -c blue,green \"python backend/run.py\" \"npm.cmd --prefix frontend run dev -- --host 127.0.0.1 --port 5173\"",
    "build": "npm.cmd --prefix frontend run build",
    "preview": "npm.cmd --prefix frontend run preview"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
